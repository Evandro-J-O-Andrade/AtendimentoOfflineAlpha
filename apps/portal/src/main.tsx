import React from 'react'
import ReactDOM from 'react-dom/client'
import { EnterpriseShell } from './shell'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <EnterpriseShell />
  </React.StrictMode>
)