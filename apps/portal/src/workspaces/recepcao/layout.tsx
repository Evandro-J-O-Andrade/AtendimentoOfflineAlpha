/**
 * @fileoverview Componente de layout do workspace de Recepção.
 * @description Define a estrutura visual e funcional do roteamento do portal
 * para usuários do workspace de Recepção, incluindo navegação específica para
 * atendimento, registro e triagem de pacientes.
 * @component RecepcaoWorkspaceLayout
 * @architecture Core workspace (role-based) — Portal da recepção
 * @dependencies React, React Router, componentes compartilhados do portal
 */

export default {}
